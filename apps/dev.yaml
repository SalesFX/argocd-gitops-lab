apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-nginx-dev
  namespace: argocd
spec:
  project: argocd-gitops-lab
  source:
    repoURL: https://github.com/SalesFX/argocd-gitops-lab
    targetRevision: HEAD
    path: dev/app
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
